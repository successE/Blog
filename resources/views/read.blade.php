@include('inc.header')
    <div class="container">
     <div class="row">
      <legend>Read Article</legend>
      <p class="lead">{{ $users->title}}</p>&nbsp;&nbsp;
      <p class="lead">{{ $users->Descriptioin}}</p>
     </div>
    </div>

@include('inc.footer')